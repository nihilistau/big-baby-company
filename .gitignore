node_modules
dist
.DS_Store
*.local
.env
*.tmp

# Local dev-server config for the editor's preview pane, not a shipping file.
.claude/launch.json

# Store build artifact — regenerate with `npm run pack:itch`.
dist-itch
